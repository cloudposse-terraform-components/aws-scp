components:
  terraform:
    example/basic:
      metadata:
        component: target
      vars:
        enabled: true
        policy_statements:
          - sid: "DenyAllActions"
            effect: "Deny"
            actions:
              - "*"
            resources:
              - "*"
