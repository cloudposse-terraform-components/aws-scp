components:
  terraform:
    example/basic:
      metadata:
        component: aws-scp
      vars:
        enabled: true
        policy_statements:
          - sid: "DenyAllActions"
            effect: "Deny"
            actions:
              - "*"
            resources:
              - "*"
